<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 1️⃣ Transparent base (so background gradient shows through) -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <solid android:color="#33FFFFFF" /> <!-- 20% white -->
        </shape>
    </item>

    <!-- 2️⃣ Frosted glass gradient overlay -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <gradient
                android:angle="90"
                android:startColor="#40FFFFFF"
            android:endColor="#10FFFFFF"
            android:type="linear"
            android:useLevel="false"/>
        </shape>
    </item>

    <!-- 3️⃣ Subtle border (optional, for more glass effect) -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <stroke
                android:width="1dp"
                android:color="#55FFFFFF" /> <!-- faint white border -->
        </shape>
    </item>

</layer-list>
