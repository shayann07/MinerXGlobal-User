<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="#262329" />

    <!-- Rounded corners -->
    <corners android:radius="12dp" />

    <!-- Optional: Border (stroke) -->
    <stroke
        android:width="1dp"
        android:color="#33FFFFFF" />
</shape>
