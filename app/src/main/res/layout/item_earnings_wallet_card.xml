<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!-- Overlapping avatar -->
    <ImageView
        android:id="@+id/profileImage"
        android:layout_width="92dp"
        android:layout_height="92dp"
        android:elevation="24dp"
        android:padding="2dp"
        android:scaleType="centerCrop"
        android:src="@drawable/person"
        android:translationY="24dp"
        android:translationZ="24dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Main card (animated liquid gold) -->
    <com.minerxgloble.minerxgloble.ui.animation.LiquidGradientCard
        android:id="@+id/cardSurface"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="-46dp"
        android:clipToPadding="false"
        android:clipChildren="false"
        android:outlineAmbientShadowColor="#FF7B00"
        android:outlineSpotShadowColor="#FF7B00"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/profileImage"
        app:cardUseCompatPadding="true"
        app:cardCornerRadius="20dp"
        app:strokeWidth="0dp"
        app:cardBackgroundColor="@android:color/transparent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="18dp"
            android:paddingTop="56dp"
        android:paddingEnd="18dp"
        android:paddingBottom="26dp">

        <TextView
            android:id="@+id/tvAccount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/item_earnings_wallet_card_minerx_earnings_wallet"
            android:textColor="@color/black"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/earningsBalance"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="@drawable/bg_referral"
            android:ellipsize="end"
            android:paddingHorizontal="12dp"
            android:paddingVertical="8dp"
            android:singleLine="true"
            android:text="@string/item_earnings_wallet_card_n_0_00"
            android:textAlignment="center"
            android:textColor="@color/black"
            android:textSize="14sp" />
    </LinearLayout>
</com.minerxgloble.minerxgloble.ui.animation.LiquidGradientCard>

    </androidx.constraintlayout.widget.ConstraintLayout>
